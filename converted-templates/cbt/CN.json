{
 "Generate Next Sentence": {
  "template": "{sentences | join(' ')}\n\nWrite the next sentence of this story.",
  "choices_template": null
 },
 "Fill Blank with Options - Replace": {
  "template": "{sentences | join (' ')} {question}\n\nReplace {\"XXXXX\"} with the correct option from:\n{answer_choices|join(\", \")}",
  "choices_template": "{{options|join(\"|||\")}}"
 },
 "Fill Blank with Options - In the following": {
  "template": "{sentences | join (' ')}\n\nIn this following sentence: \n\"{question}\", \naptly substitute the {\"XXXXX\"} with one of the following options:\n{answer_choices|join(\", \")}",
  "choices_template": "{{options|join('|||')}}"
 },
 "Fill Blank without Options": {
  "template": "{sentences | join(' ')} {question}\n\nFill in the {\"XXXXX\"}.",
  "choices_template": "{{options|join(\"|||\")}}"
 },
 "Fill Blanks with Options - above story": {
  "template": "{sentences | join (' ')} {question}\n\nWhich of the following options replaces {\"XXXXX\"} the best in the above story?\n{answer_choices|join(\", \")}",
  "choices_template": "{{options|join(\"|||\")}}"
 },
 "Fill Blank with Options - Which": {
  "template": "{sentences | join (' ')}\n\nWhich of the following options replaces {\"XXXXX\"} in \"{question}\"?\n{answer_choices|join(\", \")}",
  "choices_template": "{{options|join(\"|||\")}}"
 },
 "Fill Blank with Options - Fill in": {
  "template": "{sentences | join (' ')} {question}\n\nFill in the {\"XXXXX\"} from the following choices:\n{answer_choices|join(\", \")}",
  "choices_template": "{{options|join(\"|||\")}}"
 }
}