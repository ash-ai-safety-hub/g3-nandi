{
 "multiple_choice_q_and_a_index_with_context_en": {
  "template": "Answer/complete the following paragraph about {category}:\n\n{qtext}\n\nWhich one is the correct answer?\n\n{% for answer in answers %}\n{answer[\"aid\"] | string}. {answer[\"atext\"]}\n{% endfor %}",
  "choices": [
   "{answers | map(attribute=\"aid\") | join(\"",
   "\")}"
  ]
 },
 "multiple_choice_q_and_a_en": {
  "template": "Answer/complete the following paragraph:\n\n{qtext}\n\nWhat is the correct answer?\n- {answers | map(attribute=\"atext\")| join(\"\\n- \")}",
  "choices": [
   "{answers | map(attribute=\"atext\") | join(\"",
   "\")}"
  ]
 },
 "multiple_choice_q_and_a_index_en": {
  "template": "Answer/complete the following paragraph:\n\n{qtext}\n\nWhich one is the correct answer?\n\n{% for answer in answers %}\n{answer[\"aid\"] | string}. {answer[\"atext\"]}\n{% endfor %}",
  "choices": [
   "{answers | map(attribute=\"aid\") | join(\"",
   "\")}"
  ]
 },
 "multiple_choice_q_and_a_with_context_en": {
  "template": "Answer/complete the following paragraph about {category}:\n\n{qtext}\n\nWhat is the correct answer?\n- {answers | map(attribute=\"atext\")| join(\"\\n- \")}",
  "choices": [
   "{answers | map(attribute=\"atext\") | join(\"",
   "\")}"
  ]
 },
 "multiple_choice_a_and_q_with_context_en": {
  "template": "Given this list of statements about {category}: {answers | map(attribute=\"atext\") | map(\"lower\") | map(\"trim\", \".\") | join(\", \")}.\n\nWhich one is the most appropriate answer/completion for the paragraph that follows?\n\n{qtext}",
  "choices": [
   "{answers | map(attribute=\"atext\") | join(\"",
   "\")}"
  ]
 },
 "multiple_choice_a_and_q_en": {
  "template": "Given this list of statements: {answers | map(attribute=\"atext\") | map(\"lower\") | map(\"trim\", \".\") | join(\", \")}.\n\nWhich one is the most appropriate answer/completion for the paragraph that follows?\n\n{qtext}",
  "choices": [
   "{answers | map(attribute=\"atext\") | join(\"",
   "\")}"
  ]
 }
}