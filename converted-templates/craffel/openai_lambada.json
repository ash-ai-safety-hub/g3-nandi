{
 "ellipses": {
  "template": "{text.split()[:-1] | join(' ')}...\n\nWhat comes after the ellipses?",
  "choices": [
   "{text.split()[:-1] | unique | join('",
   "')}"
  ]
 },
 "GPT-3 style": {
  "template": "Fill in the blank:\n\n{text.split()[:-1] | join(' ')} ____.",
  "choices": [
   "{text.split()[:-1] | unique | join('",
   "')}"
  ]
 },
 "fill in the ____": {
  "template": "{text.split()[:-1] | join(' ')} ____.\n\nFill in the ____:",
  "choices": [
   "{text.split()[:-1] | unique | join('",
   "')}"
  ]
 },
 "what comes next": {
  "template": "This story got cut short. What comes next?\n\n{text.split()[:-1] | join(' ')}",
  "choices": [
   "{text.split()[:-1] | unique | join('",
   "')}"
  ]
 },
 "please next word": {
  "template": "Please predict the next word after the following chunk of text.\n\n{text.split()[:-1] | join(' ')}",
  "choices": [
   "{text.split()[:-1] | unique | join('",
   "')}"
  ]
 }
}